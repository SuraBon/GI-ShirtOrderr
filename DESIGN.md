# Design Guide

เอกสารนี้กำหนดแนวทาง UX/UI ของ GI Shirt Order ให้สอดคล้องกับ flow ล่าสุดใน `README.md` และ product rules ใน `PRODUCT.md`

## หลักการออกแบบ

- เป็นเครื่องมือทำงานภายใน ไม่ใช่ landing page
- ข้อมูลต้องอ่านเร็ว ใช้ซ้ำได้ทุกวัน และไม่ตกแต่งจนรบกวนงาน
- ใช้คำสั้น ตรงงาน และเหมือนกันทุกหน้า
- ตารางและการ์ดต้องไม่ล้นจอ โดยเฉพาะหน้าแอดมินบนมือถือ
- การกระทำที่มีผลกับข้อมูลจริงต้องมี feedback ชัดเจน
- การลบและการเปลี่ยนแปลงสำคัญต้องมี confirm หรือ toast แจ้งผล

## โทนภาพรวม

- Font: Noto Sans Thai เป็นหลัก
- พื้นหลัง: เทาอ่อน
- Surface: การ์ดขาว
- Border: เส้นบาง
- Header admin: navy
- Radius: 8-14px เป็นค่าหลัก
- Shadow: เบา ใช้เพื่อแยกระดับ ไม่ใช้เพื่อความตกแต่ง

## คำใน UI

ใช้คำเหล่านี้ให้ตรงกันทุกจุด

- `ภาพรวม`
- `รายการเบิก`
- `ข้อมูลพนักงาน`
- `แบบเสื้อและสต๊อก`
- `จัดการสาขา`
- `คำสั่งเบิก`
- `รายการเบิก`
- `รอจัดส่ง`
- `จัดส่งแล้ว`
- `ยกเลิก`
- `สต๊อกคงเหลือ`
- `เบิกแล้ว`
- `รับเข้า`
- `ปรับลด`

หลีกเลี่ยงคำสถานะอื่น เช่น `รอของ`, `กำลังดำเนินการ`, `บางส่วน` ในระดับ batch

## โครงสร้างหน้า Order

- หน้าแรกต้องเป็นประสบการณ์สั่งเบิกจริง ไม่ใช่หน้าแนะนำระบบ
- แบ่ง flow เป็นข้อมูลผู้รับผิดชอบ รายการพนักงาน และตรวจสอบก่อนส่ง
- แสดง validation ใกล้ตำแหน่งที่ต้องแก้ หรือ scroll/focus ไปยังแถวที่ผิด
- ปุ่มเพิ่มแถวต้องสร้างแถวว่าง ไม่คัดลอกจากแถวล่าสุด
- quick order และ CSV import ต้องช่วยลดการคีย์ซ้ำ แต่ต้องไม่ข้าม validation
- Success screen ต้องแสดงข้อมูลที่ส่งแล้วให้ตรวจทานได้

## โครงสร้างหน้า Dashboard

### Header และ Navigation

- Header admin ใช้ navy และคงเมนูหลักไว้ชัดเจน
- เมนูใช้ label สั้น และต้อง scroll ได้บนมือถือโดยไม่ดัน viewport
- ปุ่ม logout เป็น icon/action รอง ไม่แย่งความเด่นจากเมนู

### ภาพรวม

- แสดง KPI ก่อน panel งาน
- KPI ต้องบอกงานที่ต้องทำและจำนวนสต๊อกที่เกี่ยวข้อง
- panel `งานที่ต้องทำ`, `รายการที่ควรเติม`, `สรุปสถานะ` ต้องยุบเป็นคอลัมน์เดียวเมื่อจอแคบ
- badge ใน row ต้องไม่ดัน card ออกนอกกรอบ

### รายการเบิก

- filter อยู่ด้านบนและต้องใช้งานได้เร็ว
- ตารางบน desktop ใช้คอลัมน์พอดี ไม่ลากยาวเกินจำเป็น
- มี column settings สำหรับซ่อนข้อมูลที่ไม่ต้องดู
- บนมือถือใช้ card หรือ table wrapper ที่ scroll ในกรอบเท่านั้น
- แถว pending ใช้ tone เตือนให้รู้ว่ายังต้องดำเนินการ

### ข้อมูลพนักงาน

- ใช้ข้อมูลที่ flatten จาก batch
- ต้องดูได้ว่า employee อยู่ใน batch ไหน สาขาอะไร และสถานะอะไร
- บนมือถือให้ข้อมูลเรียงเป็น card ไม่บังคับตารางกว้างเต็มหน้า

### แบบเสื้อและสต๊อก

- แยกข้อมูลเสื้อ/รายละเอียดไซส์ออกจากการปรับสต๊อก
- แสดงสรุป `สต๊อกคงเหลือ`, `เบิกแล้ว`, `รับเข้า`, `ปรับลด`
- การปรับจำนวนต้องเห็นผลทันทีและมี toast เมื่อ sync ไม่สำเร็จ
- รูปแบบเสื้อต้องเป็นภาพจริงหรือภาพสินค้า ไม่ใช้ภาพตกแต่งที่ไม่บอกสินค้า

### จัดการสาขา

- เพิ่มสาขาอยู่ด้านบน รายการสาขาอยู่ด้านล่าง
- row สาขามีปุ่มแก้ไข/ลบเป็น icon
- การลบต้อง confirm
- ถ้าชื่อซ้ำหรือว่างต้องแจ้ง error ชัดเจน

## สีและสถานะ

- `รอจัดส่ง`: warning/warm tone เพื่อบอกว่ายังต้องทำงาน
- `จัดส่งแล้ว`: green/success tone
- `ยกเลิก`: muted/gray หรือ danger-soft ถ้าเป็น action ลบ/ยกเลิก
- สต๊อกไม่พอ: error red พร้อมข้อความ เช่น `สต๊อกไม่พอ (มี 0)`
- ปุ่มหลัก: navy
- ปุ่มรอง: white surface + border
- ปุ่มลบ: red soft + red text

## Responsive Rules

- Mobile first ที่ 320px ขึ้นไป
- หน้า admin ต้องไม่มี horizontal page scroll จากการ์ดหรือ grid
- grid หลายคอลัมน์ต้องใช้ `minmax(0, 1fr)` และ breakpoint ต้อง override ได้จริง
- flex/grid child ที่มีข้อความยาวต้องมี `min-width: 0`
- badge/status pill ต้อง `shrink` หรือย้ายบรรทัดได้บนจอเล็ก
- ตารางกว้างต้องอยู่ใน container `overflow-x-auto`
- ปุ่มบนมือถือเต็มแถวได้เมื่อพื้นที่ไม่พอ
- ข้อความในปุ่มต้องไม่ชน icon หรือขอบ

## Interaction

- ทุกปุ่มต้องมี state: default, hover, active, disabled, loading
- action ที่รอ API ต้องแสดง loading toast หรือ disabled state
- error จาก API ต้องใช้ข้อความไทยที่บอกสิ่งที่ต้องทำต่อ
- keyboard focus ต้องเห็นชัด
- dialog ต้องมีปุ่มปิดและไม่ล้น viewport

## Accessibility

- ใช้ `<button>` สำหรับ action
- icon-only button ต้องมี `aria-label` หรือ `title`
- input ต้องมี label
- สีไม่ควรเป็นตัวบอกสถานะอย่างเดียว ต้องมีข้อความสถานะด้วย
- toast/error ควรอ่านข้อความแล้วเข้าใจโดยไม่ต้องดูสี

## Visual QA Checklist

- ไม่มี horizontal scroll ที่ body บน 320px, 375px, 768px, 1280px
- Dashboard overview grid ยุบเป็น 1 คอลัมน์ก่อนล้น
- Status pill ใน row ไม่ดัน card ออกนอกกรอบ
- ตาราง desktop ไม่ทับกัน และ mobile scroll อยู่ในกรอบ
- Dialog สูงไม่เกิน viewport และ content scroll ได้
- ปุ่มหลัก/รอง/ลบอยู่ตำแหน่งคาดเดาได้
- รูปภาพแบบเสื้อไม่แตก ไม่บีบ และมี fallback เมื่อไม่มีรูป
- ข้อความไทยไม่ถูกตัดผิดจนอ่านไม่รู้เรื่อง
