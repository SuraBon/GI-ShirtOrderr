# Design Guide

## แนวทางหลัก

ระบบนี้เป็นเครื่องมือทำงานภายในสำหรับคำสั่งเบิกเสื้อพนักงาน ต้องอ่านง่าย ใช้ซ้ำได้ทุกวัน และไม่ทำให้ผู้ใช้เสียเวลากับคำหรือหน้าตาที่ไม่จำเป็น

## คำที่ใช้ในระบบ

- `คำสั่งเบิก` ใช้แทน order หรือใบสั่งซื้อทั้งหมด
- `ข้อมูลพนักงาน` ใช้กับรายการคนที่เบิกเสื้อ
- `แบบเสื้อและสต๊อก` ใช้กับหน้าจัดการสินค้า ไซส์ รูปภาพ และจำนวนคงเหลือ
- `รอจัดส่ง` ใช้เมื่อยังไม่ได้จ่ายของจริง หรือของยังไม่พอ
- `จัดส่งแล้ว` ใช้เมื่อจ่ายของแล้วและระบบตัดสต๊อกแล้ว

## โครงสร้างหน้าจอ

- หน้าคนขอเบิกต้องเน้นกรอกข้อมูลเร็ว ตรวจรายการง่าย และส่งคำขอได้ชัดเจน
- หน้าแดชบอร์ดต้องเน้นตาราง ตัวกรอง และข้อมูลที่ใช้ตัดสินใจ
- หน้าสต๊อกต้องแยกข้อมูลเสื้อ รายละเอียดไซส์ และจำนวนคงเหลือให้ไม่ปนกัน
- ตารางบนจอกว้างควรใช้พื้นที่ด้วยการแบ่งคอลัมน์หรือกำหนดคอลัมน์ให้พอดี ไม่ปล่อยแถวให้ยืดเกินจำเป็น

## สถานะและสี

- `รอจัดส่ง` ใช้สีแดงหรือโทนอุ่นเพื่อบอกว่ายังต้องทำงานต่อ
- `จัดส่งแล้ว` ใช้สีเขียวเพื่อบอกว่างานจบแล้ว
- สต๊อกไม่พอใช้ข้อความชัดเจน เช่น `สต๊อกไม่พอ (มี 0)`
- หลีกเลี่ยงสถานะอื่น เช่น รอของ บางส่วน หรือกำลังดำเนินการ

## ปุ่มและการกระทำ

- ปุ่มหลักใช้คำกริยาสั้น เช่น `เพิ่มแถว`, `เพิ่มเสื้อ`, `จัดส่งแล้ว`, `รอจัดส่ง`
- การลบคำสั่งเบิกหรือแบบเสื้อต้องมีหน้าต่างยืนยัน
- ไอคอนรูปเฟืองใช้สำหรับตั้งค่าคอลัมน์ตาราง
- ปุ่มเพิ่มแถวต้องสร้างแถวว่าง ไม่คัดลอกจากแถวล่าสุด

## Responsive

- จอเล็กใช้การ์ดหรือคอลัมน์เดียว
- จอกว้างใช้ตารางหรือกริดหลายคอลัมน์เพื่อไม่ให้แถวลากยาว
- ข้อความในปุ่มและช่องข้อมูลต้องไม่ล้น ไม่ซ้อน และไม่บังข้อมูลอื่น
