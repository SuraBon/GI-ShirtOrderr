# Product Spec

GI Shirt Order คือระบบเบิกเสื้อพนักงานที่รวมคำขอเบิก การตรวจรายการ การจัดส่ง การตัดสต๊อก และการจัดการข้อมูลหลังบ้านไว้ใน workflow เดียว

## เป้าหมาย

- ลดการคีย์ข้อมูลซ้ำระหว่างผู้ขอเบิก แอดมิน และผู้ดูแลสต๊อก
- ทำให้ทุกคำสั่งเบิกมีสถานะชัดเจนและตรวจย้อนหลังได้
- ป้องกันการจัดส่งเกินสต๊อกจริง
- ให้แอดมินจัดการแบบเสื้อ ไซส์ รูปภาพ สต๊อก และสาขาได้เอง
- ใช้งานได้จริงบนมือถือโดยไม่ให้ตาราง การ์ด ปุ่ม หรือ badge ล้นจอ

## บทบาทผู้ใช้งาน

### ผู้ขอเบิก

- เปิดหน้า `/` หรือ `/order`
- กรอกบริษัท/หน่วยงาน สาขา ผู้รับผิดชอบ และเบอร์ติดต่อ
- เพิ่มรายชื่อพนักงาน เลือกเพศ แบบเสื้อ ไซส์ และจำนวน
- ใช้ quick order หรือ import CSV เพื่อลดเวลาคีย์ข้อมูล
- ตรวจสรุปรายการก่อนส่งคำขอ
- ส่งคำขอแล้วรายการจะเริ่มที่สถานะ `รอจัดส่ง`

### แอดมินรายการเบิก

- เปิด `#/dashboard` หรือ `/dashboard`
- เข้าระบบด้วย passcode
- โหลดรายการเบิกจาก Google Sheets
- ค้นหาและกรองตามสาขา เดือน สถานะ หรือคำค้น
- ดูรายละเอียด batch และรายการพนักงาน
- เปลี่ยนสถานะหรือลบคำสั่งเบิกหลังยืนยัน
- จัดส่งทั้งหมดหรือจัดส่งบางส่วน โดยระบบตรวจสต๊อกก่อนตัดจริง
- export CSV ตามช่วงเดือนและสาขา

### ผู้ดูแลสต๊อก

- จัดการแบบเสื้อ รูปภาพ field รายละเอียดไซส์ และไซส์ตามเพศ
- เพิ่มหรือลบแถวไซส์
- รับเข้าและปรับลดสต๊อก
- ตรวจยอด `สต๊อกคงเหลือ`, `เบิกแล้ว`, `รับเข้า`, `ปรับลด`
- sync config ไป Vercel Blob และ stock tab ใน Google Sheets

### ผู้ดูแลสาขา

- เพิ่ม แก้ไข และลบสาขา
- ป้องกันชื่อสาขาว่างหรือซ้ำ
- จำกัดจำนวนสาขาสูงสุด 100 รายการ
- ข้อมูลสาขาถูกใช้ทั้งหน้า order และ dashboard

## คำศัพท์มาตรฐาน

- `คำสั่งเบิก`: batch ที่ผู้ใช้ส่งเข้าระบบหนึ่งครั้ง
- `รายการเบิก`: เสื้อแต่ละแบบ/ไซส์/จำนวนในคำสั่งเบิก
- `ข้อมูลพนักงาน`: รายชื่อพนักงาน เพศ สาขา และรายการเสื้อ
- `แบบเสื้อและสต๊อก`: แบบเสื้อ รูปภาพ ตารางไซส์ และจำนวนคงเหลือ
- `สต๊อกคงเหลือ`: จำนวนที่ยังจ่ายได้
- `เบิกแล้ว`: จำนวนที่ถูกตัดจากการจัดส่ง
- `รับเข้า`: จำนวนที่เพิ่มเข้าคลัง
- `ปรับลด`: จำนวนที่ลดออกด้วยมือ

## สถานะ

ระบบใช้สถานะ 3 ค่าเท่านั้น

- `รอจัดส่ง`: ยังไม่ได้จ่ายของครบ หรือยังรอให้แอดมินดำเนินการ
- `จัดส่งแล้ว`: จ่ายของแล้วและตัดสต๊อกเรียบร้อย
- `ยกเลิก`: ไม่ต้องจัดส่งแล้ว แต่ยังเก็บประวัติไว้

สถานะอื่น เช่น รอของ กำลังดำเนินการ หรือจัดส่งบางส่วน ไม่ใช้เป็นสถานะหลักของ batch; การจัดส่งบางส่วนถูกจัดการระดับ item ด้วย `shippedQty`, `pendingQty`, `canceledQty`

## Flow ผู้ขอเบิก

1. โหลดสาขาจาก `/api/blob/branches`
2. โหลด config แบบเสื้อจาก `/api/blob/config`
3. กรอกข้อมูลผู้รับผิดชอบและสาขาจัดส่ง
4. เพิ่มพนักงานและรายการเสื้อ
5. validate ข้อมูลบริษัท เบอร์โทร และรายการพนักงาน
6. ตรวจสรุปยอดรวมตามแบบเสื้อและไซส์
7. ส่ง payload ไป `/api/order/submit`
8. API sanitize ข้อมูล บังคับสถานะเป็น `รอจัดส่ง` และส่งต่อ Google Apps Script
9. ระบบแสดง success screen

## Flow Dashboard

1. แอดมิน login ผ่าน `/api/auth/dashboard`
2. dashboard ใช้ token ใน `Authorization: Bearer`
3. โหลด batch ผ่าน `/api/dashboard/orders`
4. frontend normalize batch และ flatten เป็น employee rows
5. ผู้ใช้กรอง ค้นหา ขยายรายละเอียด และ export CSV ได้
6. action หลังบ้านส่งผ่าน `/api/dashboard/action`
7. ถ้า token หมดอายุ ระบบล้าง session และกลับหน้า login

## Flow สต๊อก

1. คำสั่งเบิกใหม่ยังไม่ตัดสต๊อก
2. ก่อนจัดส่ง ระบบโหลด config ล่าสุดเพื่อเช็กจำนวนคงเหลือ
3. ถ้าสต๊อกไม่พอ ระบบหยุดและแจ้งรายการที่ขาด
4. ถ้าสต๊อกพอ ระบบส่ง `updateStatus` หรือ `shipItems` ไป GAS
5. ระบบปรับ stock ledger ใน config
6. config ถูกบันทึกลง Vercel Blob
7. `/api/blob/config` พยายาม sync stock ไป Google Sheets ด้วย `syncStock`

## Requirement เชิงธุรกิจ

- ส่งคำขอได้เฉพาะเมื่อข้อมูลผู้รับผิดชอบและพนักงานครบ
- เบอร์โทรต้องครบตาม `PHONE_LENGTH`
- คำสั่งเบิกใหม่ต้องเริ่มที่ `รอจัดส่ง`
- การลบคำสั่งเบิก แบบเสื้อ หรือสาขาต้องมี confirm
- การจัดส่งต้องไม่ทำให้สต๊อกติดลบ
- CSV ต้องรองรับภาษาไทยใน Excel
- ข้อมูลแบบเสื้อและสาขาต้องมี fallback เพื่อให้หน้าใช้งานได้แม้ Blob ยังว่าง

## แหล่งข้อมูล

- Google Sheets: เก็บคำสั่งเบิกและประวัติรายการ
- Google Apps Script: รับ/อ่าน/แก้ข้อมูล Google Sheets
- Vercel Blob: เก็บ config แบบเสื้อ รูปภาพ และสาขา
- sessionStorage: เก็บ dashboard token ชั่วคราว
- localStorage: cache config และ version สำหรับ optimistic concurrency

## Non-goals

- ไม่ทำระบบ user account หลายสิทธิ์
- ไม่ทำ payment หรือ accounting
- ไม่ใช้ GitHub Pages สำหรับ production เพราะต้องมี `/api/*`
- ไม่ใช้สถานะย่อยจำนวนมากในระดับ batch
